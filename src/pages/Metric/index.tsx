import * as React from 'react';
import {Metric} from '../../models/Metric';

interface Props {
	metric: Metric
}

export class MetricDetails extends React.Component<Props> {
	public render() {
		return (
			<div>
				There should be graphic
			</div>
		);
	}
}
